[pytest]
# Root-level pytest covers repo tooling under scripts/. The evaluator has its
# own config at evaluators/pytest.ini with its own testpaths; the two never
# collect each other.
pythonpath = .
testpaths = scripts/tests
